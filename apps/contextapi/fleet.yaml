defaultNamespace: api
namespace: api

targetCustomizations:

  - name: production
    clusterSelector:
      matchLabels:
        role: production
    kustomize:
      dir: overlays/production
  - name: skyvafnir-dev
    clusterSelector:
      matchLabels:
        role: skyvafnir-dev
    kustomize:
      dir: overlays/skyvafnir-dev